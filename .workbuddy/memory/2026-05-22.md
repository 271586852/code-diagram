# 2026-05-22

- 10:03 续答用户关于把 GitDiagram `Import Dependencies` 改成 `dependency-graph.html`（Cytoscape.js）渲染方案的分析：补全推荐改造方案的阶段一/阶段二、改造清单、风险与建议（数据扩展、circular/orphan 计算、性能、与 Mermaid 共存策略）。本轮仅做分析答复，未修改业务代码。
- 10:18 阶段一改造落地：`bun add cytoscape @types/cytoscape`；新增 `src/features/analyze/import-graph-view.ts`（移植 dependency-graph.html 的 buildGraph/expandSearchIds/topDir 等纯函数，输出 Cytoscape elements，新增 file/folder/folder2 三种聚合、关系范围/方向、hideOrphan、onlyDynamic）；新增 `src/components/import-graph-cytoscape.tsx`（cytoscape + next-themes 主题、控制面板、tap/dbltap 高亮邻居、详情面板、Fit/Relayout/Export PNG）；改写 `src/components/import-graph-panel.tsx` 用 next/dynamic 引入 Cytoscape 组件并提供 Cytoscape/Mermaid 渲染切换。`bun run typecheck` & `bun run lint` 通过；`bun run test` 已通过用例 8/8，剩余失败为既存 jsdom + html-encoding-sniffer ESM 兼容问题（与本次改动无关）。
