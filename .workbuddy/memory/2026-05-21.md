# 2026-05-21

- 21:21 调试 GitDiagram 本地页面的通用报错：确认用户贴出的内容是 Next.js RSC Flight payload，不是接口错误；`/local` 页面以 `initialState={null}` 渲染 `RepoPageClient`，随后 `useDiagram` 会触发 `/api/generate/stream`。`Generation details + Something went wrong` 来源于 `src/components/generation-audit-panel.tsx` 和 `src/hooks/useDiagram.ts` 的兜底 catch，重点应排查 `POST /api/generate/stream` 的 Network/Console，而不是已经 200 的 `/api/analyze/import-graph`。
- 21:44 回答用户“改了什么/为什么可以了”：确认本轮排错没有修改业务代码或运行配置，只创建/更新了 `.workbuddy/memory/2026-05-21.md` 作为诊断记录；若页面恢复，多半是重启/刷新后前端读取到 `NEXT_PUBLIC_GENERATION_BACKEND=next` 等环境变量，或之前的客户端异常状态被清掉。
- 21:48 分析 `/Users/vanky/project/wedata-fusion-base/apps/wedata-studio/dependency-graph.html` 的节点关系图实现：该 HTML 使用 Cytoscape.js + `RAW.modules` 静态数据，支持目录聚合、文件级搜索一跳/全量上下游扩展、cose/concentric/breadthfirst/circle/grid 布局、节点详情和高亮邻居。当前 GitDiagram `ImportGraphPanel` 使用 MermaidChart 渲染 `graph.mermaid`，数据结构已有 nodes/edges，可改造成 Cytoscape 渲染，但需要新增 cytoscape 依赖、React 封装组件和前端构图/过滤逻辑；服务端最好返回完整图数据而不是只返回截断后的 Mermaid 图。
